spring:
  config:
    import: optional:file:.env[.properties]

  application:
    name: telemetry

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8082